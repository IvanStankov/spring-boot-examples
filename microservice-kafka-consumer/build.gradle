bootRun {
    jvmArgs=["-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=8315"]
}

dependencies {
    compile 'org.springframework.boot:spring-boot-starter-web',
            'org.springframework.kafka:spring-kafka'
}
